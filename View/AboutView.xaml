<UserControl x:Class="lab9_RPM.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="White" CornerRadius="10" Padding="30">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Иконка приложения -->
            <TextBlock Text="📚" FontSize="72" TextAlignment="Center"/>

            <!-- Название -->
            <TextBlock Text="{Binding AppName}" 
                       FontSize="28" 
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       TextAlignment="Center"
                       Margin="0,20,0,10"/>

            <!-- Версия -->
            <TextBlock Text="{Binding Version}" 
                       FontSize="16"
                       Foreground="#7F8C8D"
                       TextAlignment="Center"
                       Margin="0,0,0,5"/>

            <!-- Разделитель -->
            <Rectangle Height="2" 
                       Fill="#3498DB" 
                       Margin="0,20" 
                       Width="100"
                       HorizontalAlignment="Center"/>

            <!-- Информация о разработке -->
            <TextBlock Text="{Binding Developer}" 
                       FontSize="14"
                       Foreground="#34495E"
                       TextAlignment="Center"
                       Margin="0,5"/>

            <TextBlock Text="{Binding Year}" 
                       FontSize="12"
                       Foreground="#95A5A6"
                       TextAlignment="Center"
                       Margin="0,5"/>

            <!-- Дополнительная информация -->
            <Border Background="#ECF0F1" 
                    CornerRadius="5" 
                    Padding="15" 
                    Margin="0,30,0,0">
                <StackPanel>
                    <TextBlock Text="Особенности приложения:" 
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>
                    <TextBlock Text="• Добавление и удаление контактов"/>
                    <TextBlock Text="• Проверка дубликатов по номеру телефона"/>
                    <TextBlock Text="• Валидация формата номера"/>
                    <TextBlock Text="• ViewModel-First навигация"/>
                    <TextBlock Text="• Dependency Injection"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Border>
</UserControl>
